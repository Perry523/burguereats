/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = async function (knex) {
  await knex.raw('ALTER TABLE clients ALTER COLUMN company_id TYPE text');
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = async function (knex) {
  // Cannot safely reverse text -> uuid if data contains non-uuid values
};
