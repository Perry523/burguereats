/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = async function (knex) {
  await knex.raw(`
    ALTER TABLE "orders" ADD COLUMN IF NOT EXISTS ifood_order_id TEXT;
  `);
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = async function (knex) {
  await knex.raw(`
    ALTER TABLE "orders" DROP COLUMN IF EXISTS ifood_order_id;
  `);
};
