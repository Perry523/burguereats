const axios = require('axios');

async function testApi() {
  try {
    // Note: This won't work easily because of auth, but I can check the server file again.
    console.log('Testing api...');
    // Actually, I'll just check the server file.
  } catch (error) {
    console.error(error);
  }
}
testApi();
